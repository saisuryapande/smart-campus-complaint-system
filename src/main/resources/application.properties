# ===== SERVER CONFIGURATION =====
server.port=${PORT:8080}

# ===== DATABASE CONFIGURATION =====
# For Production (Render) - use environment variables
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:mysql://localhost:3306/complaint_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${JDBC_DATABASE_USERNAME:root}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:Surya12345@}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA CONFIGURATION =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===== THYMELEAF CONFIGURATION =====
spring.thymeleaf.cache=false

# ===== EMAIL CONFIGURATION =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:pandesaisurya9@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:gxng xceg vxqb axfa}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===== APPLICATION BASE URL =====
app.base-url=${APP_BASE_URL:http://localhost:8080}